import { FC } from "react";

const LoginForm: FC = () => {
    return (
        <div className="relative h-full w-full bg-cover bg-center" style={{ backgroundImage: "url('/images/hero.jpg')" }}>
            ggg
        </div>
    );
}

export default LoginForm;